const db = require('./database.js'); const sql = "SELECT c.*, u.fullName as creatorName, u.role as creatorRole FROM contests c LEFT JOIN users u ON c.createdBy = u.id WHERE (c.status = 'accepted' AND (c.collegeName = ? OR u.collegeName = ?) AND (LOWER(c.visibility_scope) = 'global' OR LOWER(c.scope) = 'global' OR c.department = ? OR u.role IN ('admin', 'superadmin'))) OR c.createdBy = ? ORDER BY c.id DESC"; db.all(sql, ['Meerut Institute of Technology', 'Meerut Institute of Technology', 'COMPUTER SCIIENCE', 3], (err, rows) => { if (err) console.error(err); else console.log(JSON.stringify(rows, null, 2)); });
