<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pending Questions Approval | CampusCode</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/shared.css">
    <link rel="stylesheet" href="/css/hod/style.css">
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        dark: {
                            DEFAULT: '#0f172a',
                            sub: '#1e293b',
                            lighter: '#334155'
                        },
                        primary: {
                            500: '#3b82f6',
                            600: '#2563eb'
                        }
                    }
                }
            }
        }
    </script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        body { font-family: 'Inter', sans-serif; }
        .glass { background: rgba(30, 41, 59, 0.7); backdrop-filter: blur(12px); }
        .custom-scrollbar::-webkit-scrollbar { width: 6px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #475569; border-radius: 10px; }
        .sidebar-item-active { background: rgba(59, 130, 246, 0.1); border-right: 4px solid #3b82f6; color: #60a5fa; }
    </style>
</head>
<body class="bg-dark text-slate-200 min-h-screen custom-scrollbar">
    <div class="flex flex-col min-h-screen">
        <div class="flex-1 flex flex-col overflow-hidden">
            <!-- Top Header -->
            <header class="bg-dark-sub border-b border-slate-800 h-[88px] flex items-center px-8 justify-between shrink-0">
                <div class="flex flex-col">
                    <div class="flex items-center gap-3">
                        <div class="w-8 h-8 bg-orange-500 rounded flex items-center justify-center text-white">
                            <i class="fas fa-tasks"></i>
                        </div>
                        <h2 class="text-2xl font-bold text-white">Pending Questions Approval</h2>
                    </div>
                    <p class="text-sm text-slate-400 mt-1">Review and approve questions submitted by faculty members</p>
                </div>
                <div class="flex items-center gap-3">
                    <a href="/college/hod/problem" class="px-4 py-2 bg-slate-700 hover:bg-slate-600 rounded-lg text-sm font-medium transition-colors flex items-center gap-2">
                        <i class="fas fa-arrow-left"></i> Back to Problems
                    </a>
                    <button onclick="bulkApprove()" class="px-4 py-2 bg-green-600 hover:bg-green-700 rounded-lg text-sm font-medium transition-colors flex items-center gap-2">
                        <i class="fas fa-check-double"></i> Bulk Approve
                    </button>
                    <button class="px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded-lg text-sm font-medium transition-colors flex items-center gap-2">
                        <i class="fas fa-file-export"></i> Export
                    </button>
                </div>
            </header>

            <main class="flex-1 overflow-y-auto p-8 bg-dark">
                <div class="max-w-7xl mx-auto space-y-8">
                    <!-- Stats Grid -->
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                        <div class="bg-dark-sub border border-slate-800 p-6 rounded-xl flex items-center justify-between">
                            <div>
                                <p class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-1">Total Pending</p>
                                <h3 class="text-3xl font-extrabold text-white"><%= stats.pendingCount %></h3>
                            </div>
                            <div class="w-12 h-12 bg-orange-500/10 rounded-lg flex items-center justify-center text-orange-500">
                                <i class="fas fa-clock text-xl"></i>
                            </div>
                        </div>
                        <div class="bg-dark-sub border border-slate-800 p-6 rounded-xl flex items-center justify-between">
                            <div>
                                <p class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-1">Approved</p>
                                <h3 class="text-3xl font-extrabold text-white"><%= stats.approvedCount %></h3>
                            </div>
                            <div class="w-12 h-12 bg-green-500/10 rounded-lg flex items-center justify-center text-green-500">
                                <i class="fas fa-check-circle text-xl"></i>
                            </div>
                        </div>
                        <div class="bg-dark-sub border border-slate-800 p-6 rounded-xl flex items-center justify-between">
                            <div>
                                <p class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-1">Rejected</p>
                                <h3 class="text-3xl font-extrabold text-white"><%= stats.rejectedCount %></h3>
                            </div>
                            <div class="w-12 h-12 bg-red-500/10 rounded-lg flex items-center justify-center text-red-500">
                                <i class="fas fa-times-circle text-xl"></i>
                            </div>
                        </div>
                        <div class="bg-dark-sub border border-slate-800 p-6 rounded-xl flex items-center justify-between">
                            <div>
                                <p class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-1">This Week</p>
                                <h3 class="text-3xl font-extrabold text-white"><%= stats.thisWeekCount %></h3>
                            </div>
                            <div class="w-12 h-12 bg-blue-500/10 rounded-lg flex items-center justify-center text-blue-500">
                                <i class="fas fa-calendar-alt text-xl"></i>
                            </div>
                        </div>
                    </div>

                    <!-- Main Content Area -->
                    <div class="bg-dark-sub border border-slate-800 rounded-2xl overflow-hidden shadow-2xl">
                        <!-- Tabs -->
                        <div class="px-8 pt-6 border-b border-slate-800">
                            <div class="flex items-center gap-8">
                                <button onclick="filterByStatus('all')" class="tab-btn pb-4 text-sm font-bold text-primary-500 border-b-2 border-primary-500 flex items-center gap-2" data-status="all">
                                    <i class="fas fa-list-ul"></i> All Questions
                                    <span class="bg-primary-500/20 text-primary-500 px-2 py-0.5 rounded text-[10px]"><%= allQuestions.length %></span>
                                </button>
                                <button onclick="filterByStatus('accepted')" class="tab-btn pb-4 text-sm font-medium text-slate-400 hover:text-white transition-colors flex items-center gap-2" data-status="accepted">
                                    <i class="fas fa-check"></i> Approved
                                    <span class="bg-slate-700 text-slate-300 px-2 py-0.5 rounded text-[10px]"><%= stats.approvedCount %></span>
                                </button>
                                <button onclick="filterByStatus('rejected')" class="tab-btn pb-4 text-sm font-medium text-slate-400 hover:text-white transition-colors flex items-center gap-2" data-status="rejected">
                                    <i class="fas fa-times"></i> Rejected
                                    <span class="bg-slate-700 text-slate-300 px-2 py-0.5 rounded text-[10px]"><%= stats.rejectedCount %></span>
                                </button>
                            </div>
                        </div>

                        <!-- Filters -->
                        <div class="p-8 space-y-6">
                            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                                <div class="space-y-2">
                                    <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Subject</label>
                                    <select class="w-full bg-slate-800 border border-slate-700 rounded-lg px-4 py-2.5 text-sm text-white focus:ring-2 focus:ring-primary-500 outline-none transition-all">
                                        <option value="">All Subjects</option>
                                        <% subjects.forEach(s => { %>
                                            <option value="<%= s.subject %>"><%= s.subject %></option>
                                        <% }) %>
                                    </select>
                                </div>
                                <div class="space-y-2">
                                    <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Date From</label>
                                    <div class="relative">
                                        <input type="date" class="w-full bg-slate-800 border border-slate-700 rounded-lg px-4 py-2.5 text-sm text-white focus:ring-2 focus:ring-primary-500 outline-none transition-all appearance-none">
                                        <i class="far fa-calendar-alt absolute right-4 top-1/2 -translate-y-1/2 text-slate-500"></i>
                                    </div>
                                </div>
                                <div class="space-y-2">
                                    <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Date To</label>
                                    <div class="relative">
                                        <input type="date" class="w-full bg-slate-800 border border-slate-700 rounded-lg px-4 py-2.5 text-sm text-white focus:ring-2 focus:ring-primary-500 outline-none transition-all appearance-none">
                                        <i class="far fa-calendar-alt absolute right-4 top-1/2 -translate-y-1/2 text-slate-500"></i>
                                    </div>
                                </div>
                                <div class="space-y-2">
                                    <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Approved By HOS</label>
                                    <select class="w-full bg-slate-800 border border-slate-700 rounded-lg px-4 py-2.5 text-sm text-white focus:ring-2 focus:ring-primary-500 outline-none transition-all">
                                        <option value="">All</option>
                                    </select>
                                </div>
                            </div>
                            <div class="flex items-center justify-between pt-4">
                                <div class="flex gap-4">
                                    <button class="px-6 py-2.5 bg-primary-600 hover:bg-primary-700 text-white rounded-lg text-sm font-bold flex items-center gap-2 transition-all">
                                        <i class="fas fa-filter"></i> Apply Filters
                                    </button>
                                    <button class="px-6 py-2.5 bg-slate-700 hover:bg-slate-600 text-white rounded-lg text-sm font-bold flex items-center gap-2 transition-all">
                                        <i class="fas fa-sync-alt"></i> Reset
                                    </button>
                                </div>
                                <p class="text-sm text-slate-500">Showing <span class="text-white font-bold"><%= allQuestions.length %></span> questions</p>
                            </div>
                        </div>

                        <!-- Table Area -->
                        <div class="overflow-x-auto">
                            <table class="w-full text-left border-collapse">
                                <thead>
                                    <tr class="bg-slate-800/50 text-slate-400">
                                        <th class="px-8 py-4"><input type="checkbox" id="selectAll" onclick="toggleSelectAll(this)" class="rounded border-slate-600 bg-slate-700 text-primary-500"></th>
                                        <th class="px-4 py-4 text-xs font-bold uppercase tracking-wider">Title</th>
                                        <th class="px-4 py-4 text-xs font-bold uppercase tracking-wider text-center">Difficulty</th>
                                        <th class="px-4 py-4 text-xs font-bold uppercase tracking-wider">Faculty Name</th>
                                        <th class="px-4 py-4 text-xs font-bold uppercase tracking-wider">Subject</th>
                                        <th class="px-4 py-4 text-xs font-bold uppercase tracking-wider">Head of Subject</th>
                                        <th class="px-4 py-4 text-xs font-bold uppercase tracking-wider">Approval Status</th>
                                        <th class="px-4 py-4 text-xs font-bold uppercase tracking-wider">Date Submitted</th>
                                        <th class="px-8 py-4 text-xs font-bold uppercase tracking-wider text-right">Actions</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y divide-slate-800">
                                    <% allQuestions.forEach(q => { %>
                                        <tr class="hover:bg-slate-800/30 transition-colors group q-row" data-status="<%= q.status %>">
                                            <td class="px-8 py-5 text-sm">
                                                <input type="checkbox" class="q-checkbox rounded border-slate-600 bg-slate-700 text-primary-500" value="<%= q.id %>">
                                            </td>
                                            <td class="px-4 py-5">
                                                <p class="text-sm font-bold text-white"><%= q.title %></p>
                                            </td>
                                            <td class="px-4 py-5 text-center">
                                                <% if (q.difficulty === 'Easy') { %>
                                                    <span class="px-2.5 py-1 bg-green-500/10 text-green-500 rounded text-[10px] font-bold uppercase tracking-wider">Easy</span>
                                                <% } else if (q.difficulty === 'Medium') { %>
                                                    <span class="px-2.5 py-1 bg-yellow-500/10 text-yellow-500 rounded text-[10px] font-bold uppercase tracking-wider">Medium</span>
                                                <% } else { %>
                                                    <span class="px-2.5 py-1 bg-red-500/10 text-red-500 rounded text-[10px] font-bold uppercase tracking-wider">Hard</span>
                                                <% } %>
                                            </td>
                                            <td class="px-4 py-5">
                                                <p class="text-sm font-medium text-slate-300"><%= q.facultyName %></p>
                                            </td>
                                            <td class="px-4 py-5">
                                                <p class="text-sm font-medium text-slate-400 underline decoration-slate-700 underline-offset-4"><%= q.subject %></p>
                                            </td>
                                            <td class="px-4 py-5 text-sm text-slate-400">
                                                -
                                            </td>
                                            <td class="px-4 py-5">
                                                <% if (q.status === 'accepted') { %>
                                                <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-semibold bg-green-500/10 text-green-400 border border-green-500/20">
                                                    <i class="fas fa-check-circle text-xs"></i> Accepted
                                                </span>
                                                <% } else if (q.status === 'rejected') { %>
                                                <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-semibold bg-red-500/10 text-red-400 border border-red-500/20">
                                                    <i class="fas fa-times-circle text-xs"></i> Rejected
                                                </span>
                                                <% } else { %>
                                                <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-semibold bg-amber-500/10 text-amber-400 border border-amber-500/20">
                                                    <i class="fas fa-clock text-xs"></i> Pending
                                                </span>
                                                <% } %>
                                            </td>
                                            <td class="px-4 py-5 text-sm font-medium text-slate-400">
                                                <%= new Date(q.createdAt).toLocaleDateString() %>
                                            </td>
                                            <td class="px-4 py-5">
                                                <div class="flex items-center gap-1.5">
                                                    <button onclick="openViewModal(this)"
                                                        data-title="<%= q.title %>"
                                                        data-desc="<%= q.description ? q.description.replace(/"/g, '&quot;') : '' %>"
                                                        data-input="<%= q.input_format %>"
                                                        data-output="<%= q.output_format %>"
                                                        data-constraints="<%= q.constraints %>"
                                                        data-sample-in="<%= q.sample_input %>"
                                                        data-sample-out="<%= q.sample_output %>"
                                                        class="w-8 h-8 rounded-lg bg-slate-800 flex items-center justify-center text-blue-400 hover:bg-slate-700 transition-colors" title="View Details">
                                                        <i class="far fa-eye text-xs"></i>
                                                    </button>
                                                    <button onclick="openFeedbackModal(this, '<%= q.id %>')" data-comments="<%= q.hod_comments || '' %>" class="w-8 h-8 rounded-lg bg-slate-800 flex items-center justify-center text-slate-400 hover:bg-slate-700 transition-colors" title="Message Faculty">
                                                        <i class="far fa-comment-alt text-xs"></i>
                                                    </button>
                                                    <% if (q.status === 'pending') { %>
                                                    <button onclick="updateStatus('<%= q.id %>', 'accepted')" class="w-8 h-8 rounded-lg bg-green-500/10 flex items-center justify-center text-green-400 hover:bg-green-500 hover:text-white transition-all" title="Approve">
                                                        <i class="fas fa-check text-xs"></i>
                                                    </button>
                                                    <button onclick="updateStatus('<%= q.id %>', 'rejected')" class="w-8 h-8 rounded-lg bg-red-500/10 flex items-center justify-center text-red-400 hover:bg-red-500 hover:text-white transition-all" title="Reject">
                                                        <i class="fas fa-times text-xs"></i>
                                                    </button>
                                                    <% } %>
                                                </div>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>

                        <!-- Pagination (Placeholder) -->
                        <div class="px-8 py-6 bg-slate-800/30 flex items-center justify-between border-t border-slate-800">
                            <p class="text-xs text-slate-400">Page 1 of 3</p>
                            <div class="flex gap-2">
                                <button class="w-8 h-8 rounded-lg bg-slate-800 border border-slate-700 flex items-center justify-center text-slate-400">
                                    <i class="fas fa-chevron-left text-xs"></i>
                                </button>
                                <button class="w-8 h-8 rounded-lg bg-primary-600 text-white font-bold text-xs">1</button>
                                <button class="w-8 h-8 rounded-lg bg-slate-800 border border-slate-700 text-slate-400 text-xs font-bold">2</button>
                                <button class="w-8 h-8 rounded-lg bg-slate-800 border border-slate-700 flex items-center justify-center text-slate-400">
                                    <i class="fas fa-chevron-right text-xs"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <!-- Feedback Modal -->
    <div id="feedbackModal" class="fixed inset-0 bg-black/60 backdrop-blur-sm z-[100] hidden items-center justify-center p-4">
        <div class="bg-dark-sub border border-slate-800 rounded-2xl w-full max-w-lg shadow-2xl overflow-hidden animate-in fade-in zoom-in duration-200">
            <div class="px-8 py-6 border-b border-slate-800 flex items-center justify-between">
                <div>
                    <h3 class="text-xl font-bold text-white">Faculty Feedback</h3>
                    <p class="text-xs text-slate-400 mt-1 uppercase tracking-widest font-bold">Provide corrective comments for this question</p>
                </div>
                <button onclick="closeFeedbackModal()" class="text-slate-500 hover:text-white transition-colors">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>
            <div class="p-8 space-y-6">
                <div class="space-y-3">
                    <label class="text-sm font-bold text-slate-300">Your Comments</label>
                    <textarea id="feedbackText" rows="6" class="w-full bg-slate-800 border border-slate-700 rounded-xl p-4 text-slate-200 text-sm focus:ring-2 focus:ring-primary-500 outline-none transition-all placeholder:text-slate-500" placeholder="e.g. Please clarify the second constraint. Sample 3 is incorrect."></textarea>
                </div>
                <input type="hidden" id="feedbackProblemId">
            </div>
            <div class="px-8 py-6 bg-slate-800/30 border-t border-slate-800 flex justify-end gap-3">
                <button onclick="closeFeedbackModal()" class="px-6 py-2.5 bg-slate-700 hover:bg-slate-600 text-white rounded-lg text-sm font-bold transition-all">Cancel</button>
                <button onclick="saveFeedback()" class="px-6 py-2.5 bg-primary-600 hover:bg-primary-700 text-white rounded-lg text-sm font-bold shadow-lg shadow-primary-600/20 transition-all flex items-center gap-2">
                    <i class="fas fa-paper-plane"></i> Send Feedback
                </button>
            </div>
        </div>
    </div>

    <!-- View Question Modal -->
    <div id="viewModal" class="fixed inset-0 bg-black/80 backdrop-blur-md z-[110] hidden items-center justify-center p-4 text-slate-200 font-sans">
        <div class="bg-dark-sub border border-slate-800 rounded-3xl w-full max-w-4xl shadow-2xl overflow-hidden animate-in fade-in slide-in-from-bottom-8 duration-300 max-h-[90vh] flex flex-col">
            <div class="px-10 py-8 border-b border-slate-800 flex items-center justify-between shrink-0 bg-slate-800/20">
                <div>
                    <h3 id="viewTitle" class="text-2xl font-black text-white tracking-tight">Question Details</h3>
                    <p class="text-xs text-slate-400 mt-1 uppercase tracking-[0.2em] font-bold text-primary-500">Full Specification View</p>
                </div>
                <button onclick="closeViewModal()" class="w-10 h-10 rounded-full hover:bg-slate-800 flex items-center justify-center text-slate-500 hover:text-white transition-all">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>
            
            <div class="p-10 overflow-y-auto custom-scrollbar space-y-10">
                <!-- Description Section -->
                <div class="space-y-4">
                    <div class="flex items-center gap-3">
                        <div class="w-1 h-6 bg-primary-500 rounded-full"></div>
                        <h4 class="text-sm font-black text-slate-300 uppercase tracking-widest">Problem Description</h4>
                    </div>
                    <div id="viewDesc" class="text-slate-400 leading-relaxed text-sm bg-slate-900/50 p-6 rounded-2xl border border-slate-800/50"></div>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <div class="space-y-4">
                        <div class="flex items-center gap-3">
                            <div class="w-1 h-6 bg-blue-500 rounded-full"></div>
                            <h4 class="text-sm font-black text-slate-300 uppercase tracking-widest">Input Format</h4>
                        </div>
                        <div id="viewInput" class="text-slate-400 text-sm bg-slate-900/50 p-6 rounded-2xl border border-slate-800/50"></div>
                    </div>
                    <div class="space-y-4">
                        <div class="flex items-center gap-3">
                            <div class="w-1 h-6 bg-purple-500 rounded-full"></div>
                            <h4 class="text-sm font-black text-slate-300 uppercase tracking-widest">Output Format</h4>
                        </div>
                        <div id="viewOutput" class="text-slate-400 text-sm bg-slate-900/50 p-6 rounded-2xl border border-slate-800/50"></div>
                    </div>
                </div>

                <div class="space-y-4">
                    <div class="flex items-center gap-3">
                        <div class="w-1 h-6 bg-red-500 rounded-full"></div>
                        <h4 class="text-sm font-black text-slate-300 uppercase tracking-widest">Constraints</h4>
                    </div>
                    <div id="viewConstraints" class="font-mono text-red-400/80 text-sm bg-red-500/5 p-6 rounded-2xl border border-red-500/10 whitespace-pre-wrap"></div>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <div class="space-y-4">
                        <h4 class="text-sm font-black text-slate-300 uppercase tracking-widest flex items-center gap-2">
                            <i class="fas fa-sign-in-alt text-primary-500"></i> Sample Input
                        </h4>
                        <pre id="viewSampleIn" class="bg-dark p-6 rounded-2xl border border-slate-800 text-slate-300 text-xs font-mono overflow-x-auto whitespace-pre-wrap"></pre>
                    </div>
                    <div class="space-y-4">
                        <h4 class="text-sm font-black text-slate-300 uppercase tracking-widest flex items-center gap-2">
                            <i class="fas fa-sign-out-alt text-green-500"></i> Sample Output
                        </h4>
                        <pre id="viewSampleOut" class="bg-dark p-6 rounded-2xl border border-slate-800 text-slate-300 text-xs font-mono overflow-x-auto whitespace-pre-wrap"></pre>
                    </div>
                </div>
            </div>

            <div class="px-10 py-8 bg-slate-800/20 border-t border-slate-800 flex justify-end shrink-0">
                <button onclick="closeViewModal()" class="px-8 py-3 bg-primary-600 hover:bg-primary-700 text-white rounded-xl text-sm font-black shadow-lg shadow-primary-600/20 transition-all">
                    Got it, Close
                </button>
            </div>
        </div>
    </div>

    <script>
        function filterByStatus(status) {
            const rows = document.querySelectorAll('.q-row');
            const buttons = document.querySelectorAll('.tab-btn');
            
            buttons.forEach(btn => {
                if (btn.dataset.status === status) {
                    btn.classList.add('text-primary-500', 'border-primary-500', 'font-bold');
                    btn.classList.remove('text-slate-400', 'font-medium');
                } else {
                    btn.classList.remove('text-primary-500', 'border-primary-500', 'font-bold');
                    btn.classList.add('text-slate-400', 'font-medium');
                }
            });

            rows.forEach(row => {
                const rowStatus = row.dataset.status;
                if (status === 'all' || rowStatus === status) {
                    row.style.display = '';
                } else {
                    row.style.display = 'none';
                }
            });
        }

        async function updateStatus(id, newStatus) {
            if (newStatus === 'pending') return;
            if (!confirm(`Are you sure you want to ${newStatus === 'accepted' ? 'approve' : 'reject'} this question?`)) {
                location.reload();
                return;
            }
            try {
                const res = await fetch('/college/hod/api/approve-content', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ id, type: 'question', status: newStatus })
                });
                const data = await res.json();
                if (data.success) location.reload();
                else alert(data.message || 'Update failed');
            } catch (err) {
                console.error(err);
                alert('Network error');
            }
        }

        async function saveFeedback() {
            const id = document.getElementById('feedbackProblemId').value;
            const comments = document.getElementById('feedbackText').value;
            
            try {
                const res = await fetch('/college/hod/api/update-problem-feedback', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ id, comments })
                });
                const data = await res.json();
                if (data.success) {
                    alert('Feedback sent successfully');
                    location.reload();
                } else {
                    alert(data.message || 'Error saving feedback');
                }
            } catch (err) {
                console.error(err);
                alert('Network error');
            }
        }

        function toggleSelectAll(master) {
            const checkboxes = document.querySelectorAll('.q-checkbox');
            checkboxes.forEach(cb => cb.checked = master.checked);
        }

        async function bulkApprove() {
            const selected = Array.from(document.querySelectorAll('.q-checkbox:checked')).map(cb => cb.value);
            if (selected.length === 0) {
                alert('Please select at least one question');
                return;
            }

            if (!confirm(`Are you sure you want to approve ${selected.length} questions?`)) return;

            try {
                const res = await fetch('/college/hod/api/bulk-approve-questions', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ ids: selected })
                });
                const data = await res.json();
                if (data.success) {
                    alert(data.message);
                    location.reload();
                } else {
                    alert(data.message || 'Bulk approval failed');
                }
            } catch (err) {
                console.error(err);
                alert('Network error');
            }
        }

        function openViewModal(btn) {
            document.getElementById('viewTitle').innerText = btn.dataset.title;
            document.getElementById('viewDesc').innerText = btn.dataset.desc;
            document.getElementById('viewInput').innerText = btn.dataset.input;
            document.getElementById('viewOutput').innerText = btn.dataset.output;
            document.getElementById('viewConstraints').innerText = btn.dataset.constraints;
            document.getElementById('viewSampleIn').innerText = btn.dataset.sampleIn;
            document.getElementById('viewSampleOut').innerText = btn.dataset.sampleOut;
            
            document.getElementById('viewModal').classList.remove('hidden');
            document.getElementById('viewModal').classList.add('flex');
        }

        function closeViewModal() {
            document.getElementById('viewModal').classList.add('hidden');
            document.getElementById('viewModal').classList.remove('flex');
        }

        function openFeedbackModal(btn, id) {
            const currentComments = btn.getAttribute('data-comments');
            document.getElementById('feedbackProblemId').value = id;
            document.getElementById('feedbackText').value = currentComments;
            document.getElementById('feedbackModal').classList.remove('hidden');
            document.getElementById('feedbackModal').classList.add('flex');
        }

        function closeFeedbackModal() {
            document.getElementById('feedbackModal').classList.add('hidden');
            document.getElementById('feedbackModal').classList.remove('flex');
        }

        // Initialize with all questions (All tab active)
        window.onload = () => filterByStatus('all');
    </script>
    <!-- PROFILE OVERLAY -->
        <div id="profileOverlay" class="fixed inset-0 z-[80] flex items-center justify-center bg-black/50 backdrop-blur-sm hidden">
        <div class="w-[380px] bg-white dark:bg-gray-800 rounded-3xl shadow-2xl overflow-hidden animate-[fadeIn_0.2s_ease-out]">
            <div class="bg-gradient-to-br from-[#1E4A7A] to-[#153255] relative pt-12 pb-20 px-6 text-center">
                <button id="closeProfileOverlay" class="absolute top-4 right-4 w-8 h-8 rounded-full bg-white/10 hover:bg-white/20 text-white/90 transition-all flex items-center justify-center backdrop-blur-sm">
                    <i class="fas fa-times text-sm"></i>
                </button>

                <div class="absolute left-1/2 bottom-[-48px] -translate-x-1/2">
                    <div class="relative w-24 h-24 rounded-full p-1.5 bg-white dark:bg-gray-800 ring-4 ring-[#1E4A7A]/10 dark:ring-[#1E4A7A]/30">
                        <div class="w-full h-full rounded-full bg-gradient-to-br from-blue-500 to-indigo-600 flex items-center justify-center text-white text-3xl font-bold shadow-inner">
                            <%= user.fullName.split(' ').map(n => n[0]).join('').toUpperCase().slice(0,2) %>
                        </div>
                        <div class="absolute bottom-0 right-0 w-6 h-6 bg-green-500 border-4 border-white dark:border-gray-800 rounded-full"></div>
                    </div>
                </div>
            </div>

            <div class="pt-16 pb-8 px-6">
                <div class="text-center mb-6">
                    <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-1"><%= user.fullName %></h2>
                    <p class="text-sm font-medium text-gray-500 dark:text-gray-400 mb-3"><%= user.post || 'Faculty Member' %></p>

                    <div class="flex items-center justify-center gap-2 flex-wrap">
                        <span class="px-3 py-1 rounded-full text-xs font-semibold bg-blue-50 text-blue-700 dark:bg-blue-900/30 dark:text-blue-300 border border-blue-100 dark:border-blue-800">
                            <%= user.department || 'General' %> Dept
                        </span>
                        <span class="px-3 py-1 rounded-full text-xs font-semibold bg-purple-50 text-purple-700 dark:bg-purple-900/30 dark:text-purple-300 border border-purple-100 dark:border-purple-800">
                            <%= user.role ? user.role.toUpperCase() : 'FACULTY' %>
                        </span>
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-4 mb-6">
                    <div class="p-3 rounded-2xl bg-gray-50 dark:bg-gray-700/30 border border-gray-100 dark:border-gray-700 text-center">
                        <p class="text-xs text-gray-500 uppercase font-semibold">Faculty ID</p>
                        <p class="text-sm font-bold text-gray-800 dark:text-gray-200">FAC-<%= user.id %></p>
                    </div>
                    <div class="p-3 rounded-2xl bg-gray-50 dark:bg-gray-700/30 border border-gray-100 dark:border-gray-700 text-center">
                        <p class="text-xs text-gray-500 uppercase font-semibold">Joined</p>
                        <p class="text-sm font-bold text-gray-800 dark:text-gray-200"><%= user.joiningDate || 'Unknown' %></p>
                    </div>
                </div>

                <div class="space-y-3">
                    <button id="logoutBtn" class="w-full flex items-center justify-between p-3 rounded-xl hover:bg-red-50 dark:hover:bg-red-900/20 group transition-all duration-200 border border-transparent hover:border-red-100 dark:hover:border-red-800">
                        <div class="flex items-center gap-3">
                            <div class="w-8 h-8 rounded-full bg-red-50 dark:bg-red-900/20 text-red-600 dark:text-red-400 flex items-center justify-center">
                                <i class="fas fa-sign-out-alt text-sm"></i>
                            </div>
                            <span class="text-sm font-medium text-gray-700 dark:text-gray-200 group-hover:text-red-600 dark:group-hover:text-red-400">Logout</span>
                        </div>
                        <i class="fas fa-chevron-right text-xs text-gray-400 group-hover:text-red-500 dark:group-hover:text-red-400"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/Hod/index1_logic.js"></script>
</body>

</html>

